<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta charset="utf-8" />
  </head>
  <body>
    <h2>{{ $new_report['name']}}</h2>
    <p>{{ $new_report['mobile'] }}</p>
    <p>{{ $new_report['email'] }}</p>
    <p>{{ $new_report['report'] }}</p>
  </body>
</html>